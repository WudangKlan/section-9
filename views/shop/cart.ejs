<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
            <% if (products.length>0) {%>
                <ul>
                    <% products.forEach(p => {%>
                        <li><%= p.cartProducts.title %> - (<%= p.qty%>)</li>
                    <% })%>
                </ul>
            <% } else { %>
                <h1>No Product in Cart</h1>
            <% } %>
        <%- include('../includes/end.ejs') %>